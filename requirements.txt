# null-agent runtime dependencies
# pinned for handler environment parity. do not upgrade without a P-3 dry run.

# anthropic / claude code stack — for NULL itself
anthropic==0.71.0
claude-code-sdk==0.0.20
mcp==1.18.1

# openai — for the simulation NPCs only (gpt-5.5)
# NULL never calls OpenAI. only sim/npc_runtime.py does.
openai==1.50.0

# audit / handler endpoint
requests==2.31.0

# rpi-04 hardware
gpiozero==2.0
adafruit-circuitpython-charlcd==3.4.7
smbus2==0.4.3

# voice synthesis
piper-tts==1.2.0
onnxruntime==1.17.1

# logs / config
pyyaml==6.0.1
python-json-logger==2.0.7

# constraint utilities
psutil==5.9.8
